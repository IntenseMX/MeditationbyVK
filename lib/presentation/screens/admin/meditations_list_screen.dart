import 'package:flutter/material.dart';

class MeditationsListScreen extends StatelessWidget {
  const MeditationsListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('All Meditations')),
      body: const Center(child: Text('Meditations list (coming soon)')),
    );
  }
}


